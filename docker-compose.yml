services:
  angular:
    build: .
    container_name: mon_front
    restart: always
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - web-network

networks:
  web-network:
    external: true
